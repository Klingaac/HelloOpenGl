#version 330 core

layout (location = 0) in vec3 aPos;
layout (location = 1) in vec3 aColor;

out vec4 vertexColor;

uniform float scaler;

void main()
{
	// vertex color is a color assing manually to this specific vertex
	vertexColor = vec4(aColor, 1.0f);
	gl_Position = vec4(aPos.x * scaler, aPos.y * scaler, aPos.z * scaler, 1.0f);
}